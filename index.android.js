import React from 'react';
import { AppRegistry } from 'react-native';
import App from './src/App';

export default app8 = (props) => (
    <App />
);
  

AppRegistry.registerComponent('app8', () => app8);
